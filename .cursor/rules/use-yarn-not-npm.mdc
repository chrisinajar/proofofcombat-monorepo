---
description: Always use yarn and not npm
globs: 
---
- We do not use the `npm` or `npx` commands.
- Use `yarn` and its ecosystem for all node interractions
- Read through package.json files to find runnable scripts for `yarn`
